fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic[email]==2.10.3
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
python-dotenv==1.0.1
# Orientation bulk upload (Section 28 extension) — reads/writes .xlsx for the
# candidate bulk-upload endpoint and its downloadable template. Pure-Python,
# no native/compiled extensions — deliberately not pandas (far heavier, and
# nothing else in this codebase needs a dataframe library).
openpyxl==3.1.5
Pillow==11.0.0
reportlab==4.2.5
qrcode==8.0
httpx==0.28.1
aiofiles==24.1.0
# PPW Phase 3 — Official PPW Document/PDF Generation. AMS-owned HTML->PDF
# pipeline (Jinja2 template + Playwright/Chromium), independent of eFMS's own
# copy of these same libraries (eFMS/backend/requirements/base.txt) — no
# shared installation, no cross-project dependency.
Jinja2==3.1.6
playwright==1.62.0
